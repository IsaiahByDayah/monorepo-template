{
  "extends": "@repo/typescript-config/nextjs.json",
  // NOTE: These are always relative to the file, so must re-declare in extended configs
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
